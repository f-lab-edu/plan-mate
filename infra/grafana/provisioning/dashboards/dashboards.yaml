apiVersion: 1

providers:
  - name: PlanMate
    orgId: 1
    folder: PlanMate
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
