package com.planmate.trip.domain;

public enum TravelPace {
    /**
     * 방문 장소 수를 줄이고 휴식 시간을 넉넉히 둔다.
     */
    RELAXED,

    /**
     * 방문과 휴식의 균형을 맞춘다.
     */
    BALANCED,

    /**
     * 가능한 많은 장소를 촘촘하게 방문한다.
     */
    PACKED
}
